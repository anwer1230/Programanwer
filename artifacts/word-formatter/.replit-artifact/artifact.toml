kind = "web"
previewPath = "/"
title = "منسّق مستندات وورد"
version = "1.0.0"
id = "artifacts/word-formatter"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/word-formatter run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/word-formatter", "run", "build" ]
publicDir = "artifacts/word-formatter/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
