{
  "name": "workspace",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm --filter @workspace/word-formatter run build",
    "typecheck": "pnpm --filter @workspace/word-formatter run typecheck"
  }
}
